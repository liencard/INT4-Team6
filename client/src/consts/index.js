const ROUTES = {
  home: '/',
  dashboard: '/dashboard',
  ancestors: '/ancestors',
  map: '/map',
  // conversation: { path: '/:id', to: '/' },
  // userDetail: { path: '/users/:id', to: '/users/' },
  // groupDetail: { path: '/groups/:id', to: '/groups/' },
};

export { ROUTES };
